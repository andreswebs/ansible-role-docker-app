{# Compose's dotenv parser takes the rest of the line as the value, so bare
   values need no quoting. Sorted so a reordered app_env is not a file change. #}
{% for key, value in app_env | dictsort %}
{{ key }}={{ value }}
{% endfor %}
